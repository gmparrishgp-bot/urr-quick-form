name: URR train numeric handwriting engine
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Numeric training is now part of the combined URR handwriting release gate."
